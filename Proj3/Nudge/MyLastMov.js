class MyLastMov {
    constructor(row, col, direction) {
        // coords of move
        this.row = row;
        this.col = col;
        // direction where pieces were moved
        this.direction = direction;
    }
}