/**
 * Keyframe
 */
class MyKeyframe {
    constructor(instant, translateArr, rotateArr, scaleArr) {
        this.instant = instant;
        this.translate = translateArr;
        this.rotate = rotateArr;
        this.scale = scaleArr;
    }
}